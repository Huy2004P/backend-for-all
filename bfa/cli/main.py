"""
Command-line interface entry point for Backend for All.

Future CLI commands to be supported:
- bfa init: Initialize a new BFA project or service
- bfa dev: Start local development environment with hot-reloading
- bfa run: Execute services or runtime coordinators
- bfa build: Compile contracts, schemas, or deployment artifacts
- bfa test: Run service test suites across languages
- bfa service: Manage and inspect registered services
- bfa plugin: Install, list, or configure runtime plugins
- bfa generate: Generate language SDK bindings from schemas
- bfa deploy: Package and deploy services to target environments
"""

import sys


def main() -> None:
    """CLI main entry point skeleton."""
    print("Backend for All (BFA) CLI — Under development.")
    print("Run 'bfa --help' once commands are implemented.")


if __name__ == "__main__":
    main()
