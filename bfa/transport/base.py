"""
Base transport abstraction for Backend for All.

This module will define the abstract interface and contracts that all BFA
transport adapters (HTTP, gRPC, WebSocket, IPC, etc.) must implement.
"""
