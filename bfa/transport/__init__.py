"""
BFA Transport layer.

This subpackage contains transport abstractions and implementations (HTTP, gRPC, IPC)
for sending and receiving protocol messages across networks and process boundaries.
"""
